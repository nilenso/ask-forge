[project]
name = "ask-forge-eval"
version = "0.1.0"
description = "Evaluation framework for ask-forge"
requires-python = ">=3.10"
dependencies = [
    "datasets",
    "python-dotenv",
    "flask",
    "requests",
]

[tool.uv]
package = false
